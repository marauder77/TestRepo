Branching Exercises 1. Git checkout BRANCH_NAME2. Fast-forward is when git can tell without a problem when one commit was done on top of another, and sets it chronologically3. Git checkout4. Git branch -d Name_of_Branch5. Typically, it’s when two different developers comit the same information and try to merge them at the same time. Git can’t figure out which one to go with, so it returns a merge conflict for it to be resolved. 