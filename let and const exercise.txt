1. The difference is that var can be redeclared but let cannot.2. Const can’t be reassigned and redeclared compared to var. 3. Const can’t be reassigned and redeclared but let can be reassigned only. 4. When using var, it is brought up to the top of the script and called, which may return undefined if the var isn’t actually declared with any information. 